*[한국어](ANIMA_FIELD_LOOP_V4_CONSTITUTION.md) | [English](ANIMA_FIELD_LOOP_V4_CONSTITUTION.en.md)*

# ANIMA Field Loop V4 — Constitution

**Drafted**: 2026-05-02
**Status**: §0 v0 ratified by Junghoo (2026-05-02). **§1-A field loop + §2 absolute prohibitions ratified by Junghoo (2026-05-09)** ★. §1-B/C/D/E and §10 in drafting.
**Role**: Single point of reference for the evolution of SongRyeon as a *learning persona*. V4 layers a *day operation ↔ night evolution* split atop V3.
**Predecessor documents**: V3 constitution (LIVE LAW; §1·§2 valid until V4 §1-A ratification, after which only the field-loop portion is replaced by V4), REFORM_V1, V4 vision free-talk 11 turns (2026-05-01), V4 §1 decision sheet v3.1 (2026-05-09).
**Update trigger**: §0 v0 *does not move* (option-3 structure). §1-A is LIVE LAW. §1-B/C/D/E / §2 / §10 become LIVE LAW upon Junghoo's ratification after drafting.

---

## 0. One-Line Summary (v0, immovable)

> **SongRyeon is a persona that thinks and learns. The field loop (day) operates the persona every turn; the midnight government (night) evolves the persona every day. A plan-execute-critique trio recurses at every layer.**

### V3 §0 → V4 §0 changes

| Dimension | V3 | V4 |
|---|---|---|
| Persona definition | thinking | **thinking and learning** |
| Time axis | every turn (field loop) | **day ↔ night separation** |
| Structural principle | single thought flow (-1s → -1a → ...) | **trio recursion (at every layer)** |

The V3 thought flow (-1s/-1a/0_supervisor/2a/2b/phase_3/-1b) is dropped from §0 → unfolded in the §1 authority table.

### Core Principles (inherited from V3)

1. **Thinking belongs to -1s, planning to -1a, verification to 2b, speech to phase_3, review to -1b** (inherited from V3 §0; reaffirmed in V4 §1)
2. **Code handles tracking / safety / schema / execution / routing only. Meaning belongs entirely to the LLM**
3. **No self-evaluation** — work is never evaluated by its author
4. **Node ≠ LLM loop** — nodes are the skeleton, files are the flesh, the LLM is the thinking (V4 new, Appendix Y v0.8+v0.9)

---

## 0-X. Appendix X — §0 Evolution Roadmap

The §0 body does not move. Only code evolves. Each phase explicitly states why the §0 body still tells the truth.

### Phase 0 — Cleanup (now ~ N weeks)
**Goal**: Clean up V3 god-file remnants + prepare V4 infrastructure canvas.

Work:
- Decompose `Core/midnight_reflection.py` (5,330 lines) into 4 departments
- Slim `Core/nodes.py` (8 heuristics + compatibility wrappers + aliases)
- Dispose `Core.readiness` + `route_audit_result_v2` legacy
- Remove `normalized_goal` compatibility wrapper
- Clean up `audit_field_usage`

§0 alignment basis: prepare the infrastructure canvas for "the midnight government evolves the persona every day" (decomposition = first step of evolution).

### Phase 1 — Infrastructure (after Phase 0 graduation)
Work: dynamic markdown loading for node attributes (MemGPT pattern, v0.9) / REM Governor 23 fields → external file (option 4) / WarRoom v2 multi-seat (v1.0) / self-kernel static→dynamic (v1.1) / memory · DB tooling cleanup.

§0 alignment: learning infrastructure + trio-recursion infrastructure in place.

### Phase 2 — Learning (after Phase 1 graduation)
Work: activation-propagation prototype (v0.3) / EpisodeCluster + EpisodeDream (v0.4 + v0.6) / time-axis 3-split LLM loop / relationship-inference LLM / Night Fact Auditor + Governor Auditor (v0.5).

§0 alignment: "day inference, night learning" properly satisfied.

### Phase 3 — Integration (after Phase 2 graduation)
Work: CoreEgo option D (graph node + midnight avatar) / future node = tool strategy + past wiki + CoreEgo composite / bidirectional time / meta-thought department.

§0 alignment: Appendix Y 100% satisfied.

### Phase entry gates (strict)

| Entry | Gate |
|---|---|
| 0 → 1 | midnight modules ≥ 3 + nodes.py heuristics ≤ 2 + 189 tests OK + V4 §1 authority table draftable |
| 1 → 2 | external-file loading active + WarRoom v2 prototype + self-kernel dynamic |
| 2 → 3 | activation-propagation prototype + EpisodeCluster active + 1 week of accumulated learning |

---

## 0-Y. Appendix Y — §0 Final Vision (drunk-Junghoo 2026-05-01 dawn integrated text, immovable)

> **SongRyeon is a graph-neural-network persona. Nodes = skeleton, files = flesh, LLM = thinking. Day: 6 horizontal roles. Night: bidirectional differentiation along the time axis. A plan-execute-critique trio recurses at every layer. CoreEgo is dual-faced: a graph-node anchor + a midnight integrating commander avatar. Future thinking is a composite of tool strategy + past-wiki editing + CoreEgo. Weights are learned, activation propagates, and thoughts beget thoughts.**

### Vision four pillars

1. **Graph-neural-network persona** (v0.3) — activation propagation + weight learning. Day = inference, night = learning.
2. **Node ≠ LLM loop, attributes are external files** (v0.8 + v0.9) — Cypher nodes are skeleton only. Attributes are dynamically loaded from markdown files (MemGPT/Letta pattern).
3. **Recursive trio** (v1.0) — at every layer, planner-executor-critic. WarRoom dynamic node count.
4. **Meta-thought + bidirectional time + CoreEgo composite** (v1.1) — abolish self_kernel static repetition. Future node = composite of CoreEgo + tool strategy + past-wiki edits. Time axis bidirectional (past ↔ future mutual revision).

Appendix Y is the *future coordinate that does not move over time*. For code evolution, see Appendix X.

---

## 1. Per-Node Authority Tables

V4 §1 is split into 4 areas:
- **§1-A Field loop** (day turn loop) — *2026-05-09 LIVE* ★
- §1-B Midnight government (night evolution loop) — pending (after Phase 1 T-track review)
- §1-C Trio recursion + WarRoom v2 — pending (after CR1 dispatch)
- §1-D CoreEgo dual-face — pending (Phase 3)
- §1-E Future-node composite — pending (Phase 3)

Until §1-A ratification, V3 §1 was LIVE LAW. After §1-A ratification, **only the field-loop portion is replaced by V4**; the rest (midnight government, etc.) remains under V3 or memory vision as interim LIVE until §1-B etc. ratify.

---

## 1-A. Field-Loop Authority Table (V4 LIVE LAW, ratified by Junghoo 2026-05-09)

**Predecessor decisions**: Decision sheet v3.1 (`Orders/V4_Phase_0/V4_section1_decision_sheet_v3_1_2026_05_09.md`).
**Predecessor code**: F1 (commit `ab88e62`) + F2/F3/F4 (commit `6377a09`) + dispatch commit `d52c428`.
**Scope**: 9 field-loop nodes (-1s/-1a/2b/phase_3/-1b/0_supervisor/phase_1/WarRoom/119) + dead-letter status of legacy -1b_auditor.

### 1-A.0 V4 Junghoo Definition (codification of V3 → V4 evolution)

| Node | V3 definition | V4 definition (ratified 2026-05-09) |
|---|---|---|
| **-1s** | thought compiler | **Situation judge** — user-intent normalization + thought-flow tracking + routing + next-node constraint emission. **No goal-setting.** |
| **-1a** | planner | **Goal-setter** — receives -1s situation judgment, produces operational goal + execution plan + tool intent. **No routing** (deterministic signals only). |
| **2b** | fact judge | **Fact judge + thought critic** — fact mode + thought_critic mode auto-switching. ★ Authority extension. |

This definition is the single source for all of §1-A. The bulk node-name rename is dispatched together with §1-A ratification (V3 sheet decision ⑤).

### 1-A.1 -1s (start_gate) — Situation Judge

| Permitted | Forbidden |
|------|------|
| Situation reasoning (external-world understanding + user-intent normalization) | Direct authoring of tool arguments |
| Thought-flow tracking (s_thinking_history accumulation) | Final answer text |
| Routing decision (set next_node) | Fact verification (2b's job) |
| reasoning_budget issuance (internal LLM, decision ⑥) | Direct modification of -1a plan |
| ThinkingHandoff.v1 9-field authoring | Final-answer review (-1b's job) |
| Direct reasoning over compact analysis_report (F2) | Direct tool commands to -1a (abstract guidance only) |
| working_memory as reasoning input (X4) | **Goal-setting (-1a's job)** ★ |
| **Lightweight CoT** (deepen internal thought when info doesn't change, X4) | **Copying raw user wording verbatim as the goal** |
| **Verification + routing on second call** (after -1a, post-recursion, X6 B-1/B-3) | Routing to 119 outside sos trigger |
| **Active direct delivery routing** (simple direct-answer inputs go to phase_3 directly, X5) | Changing answer_mode itself |
| sos trigger (call 119) | |

**Inputs**: user_input + recent_context + s_thinking_history + reasoning_plan_hint + analysis_report compact view (F2) + tactical_briefing (F3, moved -1a→-1s) + working_memory (X4).

**Outputs**: `ThinkingHandoff.v1` (9 fields) — `producer / recipient / goal_state / evidence_state / what_we_know / what_is_missing / next_node / next_node_reason / constraints_for_next_node`.

★ **`goal_state` semantic codification** (to prevent confusion): `goal_state` = a compact, normalized refinement of user intent. **NOT the operational goal.** The operational goal is recorded in -1a's `strategist_goal.user_goal_core`. -1s only normalizes raw user wording; goal-setting belongs to -1a.

**Routing** (recipient/next_node Literal):
- `phase_3` — when simple direct-answer is possible (X5: bypass -1a)
- `-1a` — when goal-setting is required (normal flow)
- `phase_119` — sos
- `warroom_deliberator` — second-call verification finds deep deliberation needed (X6 B-3)
- `2b_thought_critic` — gate passed (X6, see 1-A.3)

**Second-call behavior rule** (X6 B-1/B-2.1, addressing Junghoo's gemma4 false-negative concern):
- Step 1 (verification first, MUST): re-read working_memory + recent_context + fact_cells thoroughly to judge "is the evidence really thin?" If sufficient → set next_node=phase_3 (skip recursion).
- Step 2 (routing): only if truly insufficient, decide warroom vs 2b_thought_critic.
- Input differentiator: second-call input = first ThinkingHandoff + (if any) 2b critique result. Explicitly different from the first call → small LLM (gemma4) is more likely to produce different output.

### 1-A.2 -1a (strategist) — Goal-Setter

| Permitted | Forbidden |
|------|------|
| Goal-setting (`strategist_goal` 4 fields = user_goal_core + answer_mode_target + success_criteria + scope) | **Routing decision (-1s's job)** ★ |
| Plan authoring + revision (`strategist_output`) | Self-evaluation (self-censorship) |
| First-pass labeling | Final answer text |
| Receiving -1s feedback for a new plan | Fact verification / re-judgment (2b/-1s's job) |
| Reasoning solely from ThinkingHandoff (after F3) | Direct read of analysis_report (after F3) |
| Citing fact_cells_for_strategist (F3, fact_id-based) | **Authoring `tool_request` itself** (after F4, owned by 0_supervisor) |
| Setting `delivery_readiness` signal (deterministic routing input) | Thought-flow tracking (-1s's job) |
| | Re-judging the situation |

**Inputs**: ThinkingHandoff.v1 + fact_cells_for_strategist (V4 5-field compact, F3) + working_memory + war_room + start_gate_* + tool_carryover + evidence_ledger.

**Inputs removed (F3)**: analysis_report / raw_read_report / reasoning_board / tactical_briefing — 4 keys removed from -1a's view. Fact authority fully migrated to -1s/2b.

**Outputs**: `strategist_goal` + `strategist_output` (case_theory + operation_plan + goal_lock + delivery_readiness + action_plan + response_strategy + achieved_findings + next_frontier + ...). **`tool_request` removed (F4)**.

**Routing** (-1a does no LLM judgment, deterministic signals only):
- `delivery_readiness=deliver_now` → graph routes to phase_3
- otherwise → graph routes to 0_supervisor
- structured-output failure → graph falls back to -1s (exception flow)

### 1-A.3 2b (analyzer + thought_critic) — Fact Judge + Thought Critic ★ V4 Authority Extension

V4's core vision: **embed 2b as a mid-cycle critic for -1s** (Junghoo vision 2026-05-09). Two modes auto-switch.

| Permitted (fact mode) | Permitted (thought_critic mode) ★ V4 new |
|---|---|
| Author `analysis_report` (evidences/source_judgments/usable_field_memo_facts) | Critique -1s thought flow (factual conflict / logic gap / memory omission / persona error) |
| Fill reasoning_board.fact_cells (fact_id automatically assigned by code) | Input-adaptive critique (fact_cells > 0 → integrated mode / == 0 → memory-based mode) |
| Re-judge upon -1s feedback | Output ThoughtCritique.v1 (classifications + evidence_refs) |

| Forbidden (both modes) |
|---|
| Routing decision (-1s's job) |
| Final answer text |
| Policy judgment |
| New fact lookup (no tool calls) — comparison of in-hand inputs only |

**Mode auto-switch (B-2.4 (다))**: 2b system prompt decides automatically from inputs.
- `fact_cells > 0` → **integrated critique mode** (facts + thought + memory combined)
- `fact_cells == 0` → **memory-based critique mode** (recent_context + working_memory + s_thinking_packet only)

**Inputs (fact mode)**: raw_read_report + planned_operation_contract + working_memory_packet + reasoning_board.

**Inputs (thought_critic mode)**: s_thinking_packet + recent_context + working_memory + (if any) fact_cells.

**Outputs (fact mode)**: analysis_report (evidences/source_judgments/usable_field_memo_facts).

**Outputs (thought_critic mode)**: ThoughtCritique.v1 (`hallucination_risks` / `logic_gaps` / `memory_omissions` / `persona_errors` classifications + `evidence_refs` fact_id citations + `delta` 1~2 sentences).

**Routing (thought_critic mode)**: -1s_start_gate (forced recursion). The second -1s call receives the critique and performs verification + warroom/CoT routing.

**Entry trigger (deterministic gate, B-2)**:
```
_strategist_needs_thought_recursion(state) =
    has_goal           # strategist_goal.user_goal_core is set
    AND fact_cells == 0  # truly empty case only (B-2.2 (가))
    AND no_tool_needed   # action_plan.required_tool empty + no tool_request
    # delivery_readiness dependency removed (LLM-label false negatives blocked)
```

When the gate passes → call 2b thought_critic.

### 1-A.4 phase_3 (speaker)

| Permitted | Forbidden |
|------|------|
| Author the final user-facing answer | Decide a new tool |
| Cite `current_turn_facts` + `analysis_report.evidences` | Extract new evidence |
| Convert 119 handoff packets into natural language | Leak internal workflow (phase names, slot keys, 119, budget) |
| Use `s_thinking_packet` for tone alignment | Change `answer_mode` itself |
| | Cite unapproved facts |

**Inputs**: response_strategy + delivery_packet + analysis_report + s_thinking_packet + recent_context + (if 119) rescue_handoff_packet.

**Outputs**: User-facing answer text.

**Routing**: delivery_review (forced).

### 1-A.5 -1b (delivery_review) — Post-Delivery Reviewer / Comparator ★ V4 New Naming

V3 naming: `delivery_review`. V4 official naming: **-1b** (decision ①). The legacy -1b_auditor (V3 pre-phase_3 bureaucracy) was abolished by V3 §10 7C, and revival is absolutely forbidden in V4 (1-A.9 + V4 §2 (i)).

| Permitted | Forbidden |
|------|------|
| LLM review of phase_3 answer | Authoring new `tool_query` |
| LLM-domain judgment (hallucination/omission/tone) | Reviewing -1a plan (pre-phase_3 review abolished) |
| Decide approve / remand / sos_119 | Fact verification (2b's job) |
| Per-turn rejection cap of 3 | Routing decision itself (only verdict + remand_target) |
| Comparative inspection of in-hand artifacts (comparator) | Final answer text |
| | Changing answer_mode |
| | **New fact lookup (comparator, not investigator)** |
| | fact_id invention (V4 §2 (d)) |

**Position**: One slot fixed *after* phase_3 (decision ②).

**Inputs (F2)**: final_answer + speaker_review + readiness_decision + analysis_report compact + response_strategy + rescue_handoff_packet compact + phase3_delivery_summary + **fact_cells_for_review** (V4 5-field compact).

**Outputs (F2)**: `DeliveryReview.v1` — verdict + reason + **reason_type** (enum: hallucination/omission/contradiction/thought_gap/tool_misuse) + **evidence_refs** (fact_id list) + **delta** (≤280 chars) + remand_target + remand_guidance + issues_found.

**Routing (automatic, code-owned single source)**:
- `verdict=approve` → END
- `verdict=sos_119` → phase_119
- `verdict=remand` + `reason_type ∈ {hallucination, omission, contradiction, thought_gap}` → -1s_start_gate
- `verdict=remand` + `reason_type=tool_misuse` → -1a_thinker

### 1-A.6 0_supervisor (Tool-Decision LLM) — Promoted After F4

V3: structured tool_call hub + LLM fallback only. V4 (after F4 commit): **promoted to a general-flow tool-decision LLM** (decisions ⑧⑨).

| Permitted | Forbidden |
|------|------|
| Receive -1a operation_contract → tool selection + args/queries | Semantic judgment / thought re-judgment |
| Result packaging | Final answer text (V4 §2 (h)) |
| Safety checks (refuse on violation, return no tool_calls) | Changing answer_mode |
| Routing (tool vs refusal) | Extracting new facts |
| | **fact_id invention** (V4 §2 (d)) |

**Inputs (F4)**: -1a operation_contract + ops_tool_cards + ops_node_cards + tool_carryover + **fact_cells** (V4 5-field) + **s_thinking_packet_what_is_missing** (ThinkingHandoff top-level + SThinkingPacket fallback).

**Outputs**: tool_calls (LLM general flow, 3 retries preserved).

**Routing**:
- tool_calls present → phase_1
- refusal (no tool_calls) → -1b (post-review)
- structured failure → -1a

### 1-A.7 phase_1 (Tool Execution) — Same as V3

| Permitted | Forbidden |
|---|---|
| Actual tool invocation (no LLM, executor) | Result interpretation |
| | Fact judgment |

**Inputs**: tool_calls authored by 0_supervisor.
**Outputs**: raw results (passed next to phase_2a → 2b).

### 1-A.8 WarRoom (deliberator) — Same as V3 + B-Branch Entry Trigger

| Permitted | Forbidden |
|------|------|
| -1s sos-tier deep deliberation | Routine routing |
| Opinion consensus (war_room state) | Tool calls |
| Next-generation thought experiments | Final answer text |

**Inputs**: state-level paraphrase agenda.
**Outputs**: war_room.
**Routing**: -1s_start_gate (forced).

**No-routine-use principle preserved**. V4 codifies entry triggers (X6 B-3): when -1s second call decides "deep deliberation needed" after verification → warroom_deliberator. When "lightweight critique sufficient" → 2b_thought_critic.

v2 dynamic seating + trio fractal + isolation design will be defined in **§1-C** (pending item 8).

### 1-A.9 phase_119 (rescue) — Same as V3

| Permitted | Forbidden |
|---|---|
| Author `rescue_handoff_packet` | Re-investigation / additional tool calls |
| Preserve `preserved_evidences` | Empty out analysis_report.evidences entirely |
| Separate `rejected_only` blocked items | Pre-phase_3 review |

**Entry triggers**: budget overrun / -1s sos / -1b sos_119 / hard_stop reached. The 119 enum classification (BUDGET_EXHAUSTED/TOOL_TIMEOUT/LLM_HALLUCINATION/ROUTE_DEADLOCK/DELIVERY_REVIEW_SOS, etc.) is to be set in **Phase 1 dispatch B9** (pending item 9, bundled with V4 §2 (e)).

**Inputs**: rescue_handoff_packet.
**Outputs**: rescue answer (failure boundary).
**Routing**: phase_3 (forced).

### 1-A.10 Legacy -1b_auditor (Dead Letter)

V3 pre-phase_3 bureaucracy. Abolished by V3 §10 7C. V4 §1-A explicitly forbids revival.

| Item | Body |
|---|---|
| Position | `route_audit_result_v2` (Core/graph.py) fallback only. Not in normal flow. |
| Revival | **V4 §2 (i) absolute prohibition.** |
| Disposition | Phase 0 graduation + post-V4 §1-A ratification fallback cleanup dispatch (C-track #0.9). |

---

## 1-A.11 Supplement 1 — 7 Fallback Authority Table

Live safety nets. Codifies loop-protection responsibility for JSON failures. **Not in normal flow, safety-net only.**

| fallback | Location | Protected | §1-A supplement |
|---|---|---|---|
| `_fallback_start_gate_turn_contract` | start_gate.py | -1s structured-output safety net | -1s authority supplement |
| `_base_fallback_strategist_output` | nodes.py | -1a structured-output safety net | -1a authority supplement |
| raw-reader fallback (×4) | nodes.py | 2a reader schema safety net | 2a authority supplement |
| `_fallback_response_strategy` | nodes.py | phase_3 contract safety net | phase_3 authority supplement |
| reasoning budget fallback | start_gate.py | LLM budget safety net | -1s internal tool (decision ⑥) |
| WarRoom fallback adapter | warroom/ | WarRoom authority (bundled with pending item 8) | WarRoom separate clause |

---

## 1-A.12 Supplement 2 — Routing Case Table

The body of V4 §1-A's *infinite-loop prevention + Junghoo definition codification*. 8 rows + 1 row (-1b remand) = 9 rows.

| Input situation | Tool needed? | Info change? | -1s decision | Next node |
|---|---|---|---|---|
| New user_input | — | ✅ (external) | Synthesize thought | -1a (goal-setting) |
| Tool result arrived | — | ✅ (2b new fact) | Re-judge | -1a (goal update) |
| **Direct-answer possible** (analysis_report sufficient or simple response) | X | — | Conclude | **phase_3 (direct, X5)** |
| Info insufficient + tool available | ✅ | — | Tool intent | -1a → 0_supervisor |
| Info insufficient + tool unavailable + fact_cells > 0 | X | X | -1s lightweight CoT (deepen internally, X4) | -1s self-accumulate → -1a |
| **Info insufficient + tool unavailable + fact_cells == 0** ★ B gate | X | X | Gate passed | **2b_thought_critic → second -1s** |
| Second -1s recursion (verification finds deep deliberation needed) | — | (NEW: 2b critique result) | Enter warroom | warroom_deliberator |
| Thought blocked (recursion also fails, hard_stop reached) | — | — | sos | phase_119 |
| -1b remand `thought_gap` | — | (no external) | Lightweight CoT once | -1a re-entry (repeated → phase_119) |

---

## 1-A.13 Supplement 3 — Trio-Recursion Anchor (V4 §0 Appendix Y v1.0 *first instance*)

The first instance of V4 §0 Appendix Y "trio recursion at every layer" is realized inside the -1s cycle. Used as a reference when §1-C (trio-recursion body) is drafted.

```
        -1s (plan/think = situation judge)
          ↕  (when gate passes)
2b_thought_critic (critique)  ←  recent_context + working_memory + s_thinking_packet
          ↓
        Second -1s recursion
        input = 2b critique + first ThinkingHandoff (NEW input → gemma4 different output)
          ↓ verify → phase_3 / warroom / CoT
        -1a (execute = goal-setting)
```

The structure's core points:
1. **-1s holds a critic** (2b_thought_critic). Self-evaluation prohibition (V3 §0 core) is preserved while still receiving critique.
2. **Input differentiator**: 2b critique result creates an explicit input difference for the second -1s call → automatic small-LLM (gemma4 e4b 4K) false-negative defense.
3. **Deterministic gate + LLM judgment hybrid**: the gate does not depend on LLM labels (direct application of V3 §0 "code routes, LLM means").

When §1-C generalizes this to "every layer", this anchor is the **field-loop-layer instance** reference.

---

## 1. Per-Node Authority Table — §1-B/C/D/E (Pending)

### §1-B Midnight-Government Authority Table

> **Status: pending.** Authority distribution for 4 departments (recall/present/past/future) + semantic-axis fork + 7 fallbacks.
> **Drafting trigger**: after Phase 1 T-track first review (write based on actual operating data).

### §1-C Trio Recursion + WarRoom v2

> **Status: pending.** Generalize Appendix Y v1.0 trio recursion to every layer. WarRoom v2 dynamic seating + isolation design (pending item 8).
> **Drafting trigger**: after Phase 1 CR1 dispatch is stamped (§1-A.13 anchor as reference).

### §1-D CoreEgo Dual-Face Authority Table

> **Status: pending.** Appendix Y v1.1 graph-node ↔ midnight-avatar dual face.
> **Drafting trigger**: Phase 3.

### §1-E Future-Node Composite Authority Table

> **Status: pending.** Appendix Y v1.1 CoreEgo + tool strategy + past-wiki composite.
> **Drafting trigger**: Phase 3.

---

## 2. Absolute Prohibition List (Violation = Auto-Reject)

V4 §2 = V3 §2 (24 inherited) + V4 new (12 added). Decision sheet v3.1 §7 single source. **Ratified by Junghoo 2026-05-09 (LIVE together with §1-A)**.

§2 scope:
- V3 §2 #1~#24 = LIVE as-is (verified to have no conflict with V4 §1-A).
- V4 §2 new (a)~(l) 12 = newly stamped via §1-A ratification (field-loop scope only; midnight-government additions when §1-B passes).

### 2-A. V3 §2 Inheritance (24 LIVE)

The following 24 are kept LIVE as-is from `ANIMA_FIELD_LOOP_V3_CONSTITUTION.md` §2. This document is reference only; the body is cited from the V3 constitution §2 as the single source.

- §2-1: No forced goal/slot generation from raw user_input
- §2-2: No storing internal strategic text in `working_memory.dialogue_state.active_task`
- §2-3: FieldMemoWriter cannot decide official branches
- §2-4: No user-facing `answer_not_ready`
- §2-5: Midnight policy cannot directly govern day-loop routes
- §2-6: -1b cannot author new `tool_query`
- **§2-7**: -1a cannot decide routing itself — *V4 reinforced* (bundled with V4 §2 (l), deterministic signals only)
- §2-8: -1a cannot ignore -1s thought
- §2-9: phase_3 cannot leak phase names / schema keys / 119 / budget
- **§2-10**: No more than one -1s↔-1a ping-pong on the same tool result — *V4 update*: after F4 commit, -1a→-1s fallback branch is gone. Still effective for other indirect ping-pong (e.g., -1b remand `thought_gap` → -1s).
- §2-11: No double-storage of `tool_carryover`
- §2-12: No double-storage of `response_strategy`
- §2-13: No dead sys_prompt builds
- §2-14: Deterministic fallback cannot attempt semantic classification / intent routing
- §2-15: Code heuristics cannot author final answer text
- §2-16: -1s cannot directly read -1a's plan tree (anti-echo-chamber)
- §2-17: -1s `next_direction` cannot directly command tool name/query
- §2-18: -1a self-evaluation / `finalize_recommendation` is forbidden
- §2-19: 119 cannot empty out `analysis_report.evidences` entirely
- §2-20: -1b cannot attempt review *before* phase_3 — *V4 §2 (i) reinforced*
- §2-21: When `max_total_budget` exceeded, automatic 119 entry; bypass is forbidden
- §2-22: phase_3 `answer_mode` decision priority order (119 → -1a → -1s)
- §2-23: -1b rejection cap is 3 per turn; over 3 = automatic sos_119
- §2-24: phase_3 may cite verified facts only; no fact creation

### 2-B. V4 New Prohibitions (12, Field-Loop Scope)

New absolute prohibitions stamped at V4 §1-A ratification. Violation = auto-reject. Stamped in decision sheet v3.1 §7.

#### V4 §2 (a) — No axis mixing
Time-axis / semantic-axis fork data must not be processed mixed within one cycle. Per semantic-government v0.3 decision.

#### V4 §2 (b) — No NULL persona metadata
DreamHint / SecondDream writes without `source_persona` are forbidden. Stamped at v1.6 R3. Persona metadata preservation is mandatory.

#### V4 §2 (c) — No remand_guidance format violation
Inconsistent shapes such as empty `reason_type` while filling `evidence_refs` are forbidden. F2 new.

#### V4 §2 (d) — No fact_id invention
-1b / -1a / 0_supervisor / 2b cannot cite a fact_id absent from `reasoning_board.fact_cells`. F2 stamped. fact_id is deterministically code-assigned; LLMs cite only.

#### V4 §2 (e) — No 119 enum unclassified
At 119 entry, `reason_type` / `severity` enums must not be empty. Pending item 9 (Phase 1 dispatch B9 will stamp the enum candidates: `BUDGET_EXHAUSTED` / `TOOL_TIMEOUT` / `LLM_HALLUCINATION` / `ROUTE_DEADLOCK` / `DELIVERY_REVIEW_SOS`, etc.).

#### V4 §2 (f) — No DreamHint expires_at bypass
Field-loop advisory queries cannot bypass the `archive_at IS NULL AND expires_at > now` filter. Stamped at R7.

#### V4 §2 (g) — No tool_request outside 0_supervisor
After F4, -1a / -1s / -1b cannot author `tool_request` directly. Tool decisions belong solely to 0_supervisor.

#### V4 §2 (h) — No 0_supervisor LLM final answer
After F4, 0_supervisor performs only tool selection; final judgment / answer text authoring is forbidden. answer_mode change is forbidden.

#### V4 §2 (i) — No legacy -1b_auditor revival
Pre-phase_3 -1b review revival is absolutely forbidden. V3 §10 7C abolition + V4 §1-A.10 dead-letter status. `route_audit_result_v2` remains as fallback only, never general flow. This clause is the V4 reinforcement of §2-20.

#### V4 §2 (j) — No ThinkingHandoff.v1 9-field omission
Routing is forbidden when any of `producer / recipient / goal_state / evidence_state / what_we_know / what_is_missing / next_node / next_node_reason / constraints_for_next_node` is empty. Fallbacks must also fill all 9.

#### V4 §2 (k) — No -1s goal-setting ★ V4 definition codification
Junghoo definition (2026-05-09): -1s is the situation judge. **Goal-setting is -1a's job.** -1s cannot copy raw user wording verbatim into `goal_state` (normalization only). -1s cannot set `strategist_goal.user_goal_core` directly (that field is -1a's single source).

This clause codifies §1-A.0 Junghoo definition + the authority boundary between ThinkingHandoff.goal_state and StrategistGoal.user_goal_core.

#### V4 §2 (l) — No -1a routing decision ★ V4 definition codification
Junghoo definition (2026-05-09): -1a is the goal-setter. **Routing is -1s's job.** -1a only emits deterministic signals (e.g., `delivery_readiness=deliver_now`); routing branches themselves are owned by code (`route_after_strategist`) as the single source.

This clause is the V4 reinforcement of §2-7 + the codification of the post-F4 disappearance of the -1a→-1s fallback branch.

### 2-C. Auto-Disposition on Violation

- (Code-domain violation) — Code-side regex/schema blocks (e.g., -1b tool-call block, ThinkingHandoff 9-field validation).
- (LLM-domain violation) — -1b comparator reports hallucination/omission/format violation (`reason_type` enum) → automatic remand_target routing (code single source).
- (Structural violation) — Code violating §2 → -1b triggers sos_119 → phase_119 entry → rescue answer.

### 2-D. V4 §2 New Candidate Free-Talk Slots (Added During Phase 1)

§2 body is the single source as of §1-A ratification. New violation patterns discovered during Phase 1 are appended to §2-E. Current pending free-talk slots = `Orders/V4_Phase_0/V4_section1_decision_sheet_v3_1_2026_05_09.md` §10 vision-talk blank slots.

When §1-B/C/D/E pass, area-specific §2 additions follow (midnight-government §2, trio-recursion §2, etc.).

---

## 10. Implementation Schedule (Pending)

> **Status: pending.** V4 §10 = Phase 0~3 implementation stages.
>
> Phase 0 dispatch sequence is in Appendix X. Dispatch progress is tracked from `ANIMA_ARCHITECTURE_MAP.md` purge log #54 (V4 Phase 0 start marker).

### V4 implementation stages (preview)

- §10-A: Phase 0 cleanup dispatches #0.1~#0.10
- §10-B: V4 §1 authority-table drafting/ratification (in parallel with Phase 0)
- §10-C: V4 §2 absolute-prohibitions drafting/ratification (in parallel with Phase 0)
- §10-D: Phase 1 infrastructure dispatches #1.1~
- ...

---

## 11. Roles (identical to V3 §11 + AGENTS.md §2)

| Role | Owner |
|---|---|
| Vision decisions / constitutional amendment | Junghoo (legislature) |
| Vision discussion + code diagnosis + Codex review | Claude (judicial advisory) |
| Code authoring/modification/testing | Codex (executive implementation) |
| Final approval (merge) | Junghoo |

V4 addition: *divided collaboration mode* — drunk Junghoo (vision broadcasting) ↔ sober Junghoo (review) ↔ Claude (code-coordinate auto-attachment) ↔ Codex (implementation).

---

## Appendix A — Decision Queue (at V4 §0 ratification, 2026-05-02)

A total of 29 items were identified during the V4 vision free-talk 11 turns. Priorities:

**Top priority**:
- ✅ #1 V4 §0 one-line confirmed (B3 + option-3 structure, ratified 2026-05-02)
- 🔜 #2 REM Governor option 4 (external file) adoption
- 🔜 #25 CoreEgo position option (A/B/C/D)
- 🔜 #24 Bidirectional time-axis flow decision
- 🔜 #26 Future-node 3-element composite decision
- 🔜 #23 self_kernel evolution decision

**Structural decisions**:
- #3 Strategy Council abolition, #4 Person+CoreEgo entry, #5 Top-tier structure scenarios A/B/C/D, #16 Recursive trio in V4 §0, #17 Sub-agent depth limits, #18 Sub-schema external-file location, #27 Meta-thought department, #28 Revision count cap

**Implementation (new)**: #6 EpisodeCluster, #7 Time-axis 3-split, #8 Relationship-inference LLM, #9 Night Fact Auditor, #10 Governor Auditor, #11 Activation-propagation prototype, #19 Sub-agent LLM model, #20 Midnight v1.0 application, #21 Core/warroom v1.0 absorption, #22 ANIMA_WARROOM_V2_SCHEMA update, #29 self_kernel staged-abolition roadmap

**Documentation/maintenance**: #12 V4 §1 drafting, #13 V4 §2 drafting, #14 midnight_reflection slimming (= Phase 0 A-track), #15 7 ghost-department dispositions

---

## Appendix B — Phase 0 Dispatch Five Axes (consensus 2026-05-02)

1. **Standard format**: Why / Scope / Code coordinates / Verification criteria / Rollback / §-alignment / Dependencies
2. **Unit**: 1 dispatch = 1 PR = 1 purge-log line (V3 implementation-stage equivalent, code 50~500 lines, files 1~5)
3. **Sequence**: parallel — A-track (midnight decomposition) ‖ §1·§2 drafting → B-track (nodes.py slimming) → C-track (legacy removal) → D (tooling cleanup)
4. **Graduation gate**: strict — Phase 0 → 1 gate (see Appendix X table)
5. **Tracking**: continue from `ANIMA_ARCHITECTURE_MAP.md` purge log #54 + V4 Phase 0 start marker line

### Phase 0 dispatch sequence

```
[A-track] midnight_reflection.py decomposition (immediately startable, no §3.3 trigger)
  #0.1 First decomposition (first department)
  #0.2 Second decomposition
  #0.3 Third decomposition
  #0.4 Fourth decomposition (closure)

[§1·§2 drafting] V4 §1 authority table + V4 §2 absolute prohibitions (Junghoo + Claude vision discussion)

[B-track] Core/nodes.py slimming (after V4 §1·§2 ratification — AGENTS.md §3.3 gate)
  #0.5 Fallback heuristic 8 purge
  #0.6 Compatibility wrapper removal
  #0.7 normalized_goal alias removal

[C-track] Legacy removal (after A·B done)
  #0.8 Core.readiness disposition
  #0.9 route_audit_result_v2 disposition

[D] Tooling cleanup
  #0.10 audit_field_usage (any time)
```

### Per-dispatch gates (per PR)

- 189 tests OK maintained
- ANIMA_ARCHITECTURE_MAP.md purge-log line added
- No V4 §0 v0 violations (think / learn / evolve principles)
- No V3 §2 24-prohibition violations
- (where applicable) no V4 §2 absolute-prohibition violations

---

**Version**: V4 §0 v0 (2026-05-02) + **§1-A v1 (2026-05-09)** + **§2 v1 (2026-05-09)** ★
**Next update**: §1-B drafting (after Phase 1 T-track review) or §2 new violation pattern addition.
